<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content />
    <meta name="keywords" content />
    <title>Balu Cabs</title>
    <link rel="icon" type="image/x-icon" href="{{ asset('frontend/assets/img/logo/logo1.png') }}" />
    <link rel="stylesheet" href="{{ asset('frontend/assets/css/bootstrap.min.css') }}" />
    <link rel="stylesheet" href="{{ asset('frontend/assets/css/all-fontawesome.min.css') }}" />
    <link rel="stylesheet" href="{{ asset('frontend/assets/css/flaticon.css') }}" />
    <link rel="stylesheet" href="{{ asset('frontend/assets/css/animate.min.css') }}" />
    <link rel="stylesheet" href="{{ asset('frontend/assets/css/magnific-popup.min.css') }}" />
    <link rel="stylesheet" href="{{ asset('frontend/assets/css/owl.carousel.min.css') }}" />
    <link rel="stylesheet" href="{{ asset('frontend/assets/css/jquery-ui.min.css') }}" />
    <link rel="stylesheet" href="{{ asset('frontend/assets/css/nice-select.min.css') }}" />
    <link rel="stylesheet" href="{{ asset('frontend/assets/css/style.css') }}" />
    <link rel="stylesheet" href="{{ asset('frontend/assets/css/mobile-view.css') }}" />

    <link rel="stylesheet" href="{{ asset('frontend/assets/css/calendar.css') }}" />

    <link rel="stylesheet" href="{{ asset('frontend/assets/css/mobile-menu.css') }}" />


    <!-- Mobile Banner css start --->
    <link rel="stylesheet" href="{{ asset('frontend/assets/css/mobile-banner.css') }}" />
    <!-- Mobile Banner CSS END --->

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css" />
    <style>
        .banner-btn {
            bottom: 4%;
        }

        .icon-sidebar-desk {

            position: fixed;
            bottom: 7%;
            right: 0;
            background-color: none;
            z-index: 99999;
            padding: 5px
        }

        .airport-name .row:not(:last-child) {
            margin-bottom: 15px;
        }


        .city-landmark .row:not(:last-child) {
            margin-bottom: 15px;
        }


        .submit-button {
            background-color: var(--theme-color);
            /* Primary color */
            color: white;
            /* Text color */
            border: none;
            /* Removes the default border */
            padding: 10px 20px;
            /* Adjusts the padding around the text */
            border-radius: 5px;
            /* Rounds the corners of the button */
            cursor: pointer;
            /* Changes the cursor on hover */
            transition: background-color 0.3s ease;
            /* Smooth transition for hover effect */
        }

        .submit-button:hover {
            background-color: #0056b3;
            /* Darker shade for hover effect */
        }

        img {
            vertical-align: middle
        }

        .icon-sidebar-desk {
            position: fixed;
            bottom: 8%;
            right: 2.5%;
            background-color: none;
            z-index: 99999;
            padding: 5px;
        }
    </style>



</head>

<body>
